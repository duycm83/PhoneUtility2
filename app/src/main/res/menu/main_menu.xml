<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/itemNewFile"
        android:icon="@drawable/ic_note_add_white_48dp"
        android:title="@string/new_file"
        app:showAsAction="always"
        />
<!--     <item -->
<!--         android:id="@+id/itemAbout" -->
<!--         android:icon="@drawable/ic_menu_help" -->
<!--         android:title="@string/about"/> -->
    
    <item android:id="@+id/itemUp"
        android:icon="@drawable/ic_arrow_up"
        android:title="@string/up_dir"
        app:showAsAction="always"
        android:onClick="upToParentDir"/>
	<item android:id="@+id/itemSearch"
         android:title="Search"
         android:icon="@android:drawable/ic_menu_search"
         app:showAsAction="always"/>
</menu>